# ────────────────────────────────────────────────────────
# Direct dependencies — every package below is imported
# somewhere in the production codebase (tests excluded).
# ────────────────────────────────────────────────────────

# API framework
fastapi>=0.110.0
uvicorn>=0.27.0

# HTTP client (used everywhere for external APIs)
httpx>=0.27.0

# Data validation / models
pydantic>=2.6.0
pydantic-settings>=2.1.0

# JWT authentication
pyjwt>=2.8.0

# Multipart form-data parsing (FastAPI UploadFile)
python-multipart>=0.0.9

# Vector DB (Qdrant) — core for retrieval
qdrant-client>=1.7.0

# Metrics (Prometheus) — core for observability
prometheus-client>=0.19.0

# LlamaIndex — core for indexing/reranking
llama-index>=0.10.0

# ────────────────────────────────────────────────────────
# Optional / feature-gated dependencies
# Install only if you enable the corresponding feature.
# ────────────────────────────────────────────────────────

# Document parsing — required for ingestion pipelines
python-docx>=1.1.0     # .docx files
openpyxl>=3.1.0        # .xlsx files
python-pptx>=0.6.23    # .pptx files
pdfplumber>=0.10.0     # PDF parsing (MIT licensed)
markdown>=3.5.0        # .md → HTML conversion
python-magic>=0.4.27   # MIME type detection
pyyaml>=6.0.0          # YAML config files
docling>=2.0.0         # Universal document parser
pillow>=10.0.0        # Image processing for diagrams

# Embeddings — required for local embedding generation
sentence-transformers>=2.2.0
llama-index-embeddings-huggingface>=0.7.0

# Redis — required for distributed caching
redis>=5.0.0

# Vision models — required for multimodal
torch>=2.0.0
torchvision>=0.15.0

# Language detection — required for multilingual documents
langdetect>=1.0.9

# ColBERT — late interaction embeddings (optional, enables COLBERT_ENABLED=true)
fastembed>=0.3.0
