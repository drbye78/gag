apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: eis

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - secrets.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/name: eis
  app.kubernetes.io/version: "3.2.0"
