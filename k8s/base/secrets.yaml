apiVersion: v1
kind: Secret
metadata:
  name: eis-secrets
  namespace: eis
  labels:
    app.kubernetes.io/name: eis
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # CRITICAL: Set these values before deploying via sealed secrets or external secrets operator
  # Deploy will fail if these are not changed from empty values
  JWT_SECRET: ""
  CREDENTIAL_ENCRYPT_KEY: ""
  LLM_API_KEY: ""
  # Database credentials (if using external DB)
  # QDRANT_API_KEY: ""
  # FALKORDB_PASSWORD: ""
