apiVersion: v1
kind: Secret
metadata:
  name: eis-secrets
  namespace: eis
  labels:
    app.kubernetes.io/name: eis
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # Set these values in production via sealed secrets or external secrets operator
  JWT_SECRET: "change-me-in-production"
  CREDENTIAL_ENCRYPT_KEY: "32-character-encryption-key-here"
  LLM_API_KEY: ""
  # Database credentials (if using external DB)
  # QDRANT_API_KEY: ""
  # FALKORDB_PASSWORD: ""
