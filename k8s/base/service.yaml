apiVersion: v1
kind: Service
metadata:
  name: eis-api
  namespace: eis
  labels:
    app.kubernetes.io/name: eis-api
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: eis-api
